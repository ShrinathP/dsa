
Problem Statement
A frog wants to climb a staircase with n steps.
Given an integer array heights, where heights[i] contains the height of the ith step, and an integer k. 
To jump from the ith step to the jth step, the frog requires abs(heights[i] - heights[j]) energy, where abs() denotes the absolute difference. 
The frog can jump from the ith step to any step in the range [i + 1, i + k], provided it exists. 
Return the minimum amount of energy required by the frog to go from the 0th step to the (n-1)th step.

Cue  - top down recursion - back jumps
===========================================================
Steps
1. Initialize memo arr, fill with -1
2. recurFunction(currIndex, k, heights[], memo)
    - base cases
        -> if currIndex = 0, return 0, no way to jump back
        -> if memo[currIndex] != -1, return memo[currIndex]
    - For k jumps backwards from n-1, run a loop from j=1 to j=k
        for(j=1; j<=k; j++)
        - currCost = recurMethod(i-j, ...)
        - minCost = Math.min(minCost, currCost)
    - memo[currIndex] = minCost, return memo[currIndex]
===========================================================

import java.util.Arrays;

class Solution {
    public int frogJump(int n, int k, int[] h) {
        int[] memo = new int[n];
        Arrays.fill(memo, -1);
        return solve(n - 1, k, h, memo);
    }

    private int solve(int i, int k, int[] h, int[] memo) {
        if (i == 0) return 0;

        if (memo[i] != -1) return memo[i];

        int minCost = Integer.MAX_VALUE;

        for (int j = 1; j <= k; j++) {
            if (i - j >= 0) {
                int cost = solve(i - j, k, h, memo) 
                         + Math.abs(h[i] - h[i - j]);
                minCost = Math.min(minCost, cost);
            }
        }

        return memo[i] = minCost;
    }
}
